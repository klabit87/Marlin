name: Build Configs

on:
  schedule:
    - cron: '0 0 * * *'
  push:
    branches: [ main,dev ]
  pull_request:
    branches: [ main,dev ]
  workflow_dispatch:

jobs:
  Build-Default:
    name: Build Default Files
    runs-on: ubuntu-latest
    strategy:
      matrix:
        chip: [G32,N32]
    steps:
    - uses: actions/checkout@v2
    - name: Cache pip
      uses: actions/cache@v2
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    - name: Cache PlatformIO
      uses: actions/cache@v2
      with:
        path: ~/.platformio
        key: ${{ runner.os }}-${{ hashFiles('**/lockfiles') }}
    - name: Set up Python
      uses: actions/setup-python@v2
    - name: Run OS Update & Install PlatformIO
      run: |
        sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y
        python -m pip install --upgrade pip
        pip install wheel
        pip install --upgrade platformio
    - name: Run PlatformIO
      id: build_configs
      env:
        AQUILA_VER: ${{ matrix.chip }}
      run: |
        if [ "$AQUILA_VER" = G32 ]; then AVTMP="G32"; else AVTMP="N32"; fi;
        echo "::set-output name=filename::Default-NoProbe-${AVTMP}.bin"
        cp config/aquila_templates/Default-NoProbe/Configuration.h Marlin/Configuration.h
        cp config/aquila_templates/Default-NoProbe/Configuration_adv.h Marlin/Configuration_adv.h
        sed -i "s/default_envs =.*/default_envs = STM32F103RET6_voxelab_aquila_$AQUILA_VER/g" platformio.ini
        pio run
        mkdir temp
        mv .pio/build/STM32F103RET6_voxelab_aquila_$AQUILA_VER/*.bin temp/build.bin
    - name: Upload Build Binary
      uses: actions/upload-artifact@v3
      with:
        name: ${{ steps.build_configs.outputs.filename }}
        path: temp/build.bin
  
  Build-BLTouch:
    name: Build BL-Touch Files
    runs-on: ubuntu-latest
    strategy:
      matrix:
        chip: [G32,N32]
        grid: [3,4,5,9]
        highspeed: [true,false]
    steps:
    - uses: actions/checkout@v2
    - name: Cache pip
      uses: actions/cache@v2
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    - name: Cache PlatformIO
      uses: actions/cache@v2
      with:
        path: ~/.platformio
        key: ${{ runner.os }}-${{ hashFiles('**/lockfiles') }}
    - name: Set up Python
      uses: actions/setup-python@v2
    - name: Run OS Update & Install PlatformIO
      run: |
        sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y
        python -m pip install --upgrade pip
        pip install wheel
        pip install --upgrade platformio
    - name: Run PlatformIO
      id: build_configs
      env:
        AQUILA_VER: ${{ matrix.chip }}
        GRID: ${{ matrix.grid }}
        HS: ${{ matrix.highspeed }}
      run: |
        if ($HS); then HSTMP=-HS; else HSTMP=; fi;
        if [ "$AQUILA_VER" = G32 ]; then AVTMP="G32"; else AVTMP="N32"; fi;
        echo "::set-output name=filename::BLTouch-${GRID}x${GRID}${HSTMP}-${AVTMP}.bin"
        cp config/aquila_templates/BLTouch-3x3/Configuration.h Marlin/Configuration.h
        cp config/aquila_templates/BLTouch-3x3/Configuration_adv.h Marlin/Configuration_adv.h
        sed -i "s/default_envs =.*/default_envs = STM32F103RET6_voxelab_aquila_$AQUILA_VER/g" platformio.ini
        sed -i "s/#define GRID_MAX_POINTS_X.*/#define GRID_MAX_POINTS_X $GRID/g" Marlin/Configuration.h
        if ($HS); then TEMP=; else TEMP='\/\/'; fi;
        sed -i "s/[^ ]*#define BLTOUCH_HS_MODE/$TEMP#define BLTOUCH_HS_MODE/g" Marlin/Configuration_adv.h
        pio run
        mkdir temp
        mv .pio/build/STM32F103RET6_voxelab_aquila_$AQUILA_VER/*.bin temp/build.bin
    - name: Upload Build Binary
      uses: actions/upload-artifact@v3
      with:
        name: ${{ steps.build_configs.outputs.filename }}
        path: temp/build.bin
  
  Build-Manual-Mesh:
    name: Build Manual-Mesh Files
    runs-on: ubuntu-latest
    strategy:
      matrix:
        chip: [G32,N32]
        grid: [3,4,5]
    steps:
    - uses: actions/checkout@v2
    - name: Cache pip
      uses: actions/cache@v2
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    - name: Cache PlatformIO
      uses: actions/cache@v2
      with:
        path: ~/.platformio
        key: ${{ runner.os }}-${{ hashFiles('**/lockfiles') }}
    - name: Set up Python
      uses: actions/setup-python@v2
    - name: Run OS Update & Install PlatformIO
      run: |
        sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y
        python -m pip install --upgrade pip
        pip install wheel
        pip install --upgrade platformio
    - name: Run PlatformIO
      id: build_configs
      env:
        AQUILA_VER: ${{ matrix.chip }}
        GRID: ${{ matrix.grid }}
      run: |
        if [ "$AQUILA_VER" = G32 ]; then AVTMP="G32"; else AVTMP="N32"; fi;
        echo "::set-output name=filename::ManualMesh-${GRID}x${GRID}-${AVTMP}.bin"
        cp config/aquila_templates/ManualMesh-3x3/Configuration.h Marlin/Configuration.h
        cp config/aquila_templates/ManualMesh-3x3/Configuration_adv.h Marlin/Configuration_adv.h
        sed -i "s/default_envs =.*/default_envs = STM32F103RET6_voxelab_aquila_$AQUILA_VER/g" platformio.ini
        sed -i "s/#define GRID_MAX_POINTS_X.*/#define GRID_MAX_POINTS_X $GRID/g" Marlin/Configuration.h
        pio run
        mkdir temp
        mv .pio/build/STM32F103RET6_voxelab_aquila_$AQUILA_VER/*.bin temp/build.bin
    - name: Upload Build Binary
      uses: actions/upload-artifact@v3
      with:
        name: ${{ steps.build_configs.outputs.filename }}
        path: temp/build.bin
 
  Build-UBL-BLTouch:
    name: Build BL-Touch UBL Files
    runs-on: ubuntu-latest
    strategy:
      matrix:
        chip: [G32,N32]
        grid: [10,15]
        highspeed: [true,false]
    steps:
    - uses: actions/checkout@v2
    - name: Cache pip
      uses: actions/cache@v2
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    - name: Cache PlatformIO
      uses: actions/cache@v2
      with:
        path: ~/.platformio
        key: ${{ runner.os }}-${{ hashFiles('**/lockfiles') }}
    - name: Set up Python
      uses: actions/setup-python@v2
    - name: Run OS Update & Install PlatformIO
      run: |
        sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y
        python -m pip install --upgrade pip
        pip install wheel
        pip install --upgrade platformio
    - name: Run PlatformIO
      id: build_configs
      env:
        AQUILA_VER: ${{ matrix.chip }}
        GRID: ${{ matrix.grid }}
        HS: ${{ matrix.highspeed }}
      run: |
        if ($HS); then HSTMP=-HS; else HSTMP=; fi;
        if [ "$AQUILA_VER" = G32 ]; then AVTMP="G32"; else AVTMP="N32"; fi;
        echo "::set-output name=filename::UBL-BLTouch-${GRID}x${GRID}${HSTMP}-${AVTMP}.bin"
        cp config/aquila_templates/UBL-BLTouch-10x10/Configuration.h Marlin/Configuration.h
        cp config/aquila_templates/UBL-BLTouch-10x10/Configuration_adv.h Marlin/Configuration_adv.h
        sed -i "s/default_envs =.*/default_envs = STM32F103RET6_voxelab_aquila_$AQUILA_VER/g" platformio.ini
        sed -i "s/#define GRID_MAX_POINTS_X.*/#define GRID_MAX_POINTS_X $GRID/g" Marlin/Configuration.h
        if ($HS); then TEMP=; else TEMP='\/\/'; fi;
        sed -i "s/[^ ]*#define BLTOUCH_HS_MODE/$TEMP#define BLTOUCH_HS_MODE/g" Marlin/Configuration_adv.h
        pio run
        mkdir temp
        mv .pio/build/STM32F103RET6_voxelab_aquila_$AQUILA_VER/*.bin temp/build.bin
    - name: Upload Build Binary
      uses: actions/upload-artifact@v3
      with:
        name: ${{ steps.build_configs.outputs.filename }}
        path: temp/build.bin
  
  Build-UBL-NoProbe:
    name: Build NoProbe UBL Files
    runs-on: ubuntu-latest
    strategy:
      matrix:
        chip: [G32,N32]
        grid: [3,5]
    steps:
    - uses: actions/checkout@v2
    - name: Cache pip
      uses: actions/cache@v2
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    - name: Cache PlatformIO
      uses: actions/cache@v2
      with:
        path: ~/.platformio
        key: ${{ runner.os }}-${{ hashFiles('**/lockfiles') }}
    - name: Set up Python
      uses: actions/setup-python@v2
    - name: Run OS Update & Install PlatformIO
      run: |
        sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y
        python -m pip install --upgrade pip
        pip install wheel
        pip install --upgrade platformio
    - name: Run PlatformIO
      id: build_configs
      env:
        AQUILA_VER: ${{ matrix.chip }}
        GRID: ${{ matrix.grid }}
      run: |
        if [ "$AQUILA_VER" = G32 ]; then AVTMP="G32"; else AVTMP="N32"; fi;
        echo "::set-output name=filename::UBL-NoProbe-${GRID}x${GRID}-${AVTMP}.bin"
        cp config/aquila_templates/UBL-NoProbe-3x3/Configuration.h Marlin/Configuration.h
        cp config/aquila_templates/UBL-NoProbe-3x3/Configuration_adv.h Marlin/Configuration_adv.h
        sed -i "s/default_envs =.*/default_envs = STM32F103RET6_voxelab_aquila_$AQUILA_VER/g" platformio.ini
        sed -i "s/#define GRID_MAX_POINTS_X.*/#define GRID_MAX_POINTS_X $GRID/g" Marlin/Configuration.h
        pio run
        mkdir temp
        mv .pio/build/STM32F103RET6_voxelab_aquila_$AQUILA_VER/*.bin temp/build.bin
    - name: Upload Build Binary
      uses: actions/upload-artifact@v3
      with:
        name: ${{ steps.build_configs.outputs.filename }}
        path: temp/build.bin
